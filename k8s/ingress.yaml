apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mlops-iris-api
  namespace: mlops
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: mlops.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mlops-iris-api
                port:
                  number: 80
